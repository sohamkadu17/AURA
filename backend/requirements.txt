# AURA Backend Requirements
# Using minimum version constraints (>=) instead of exact pins to let pip
# resolve a compatible dependency graph across all packages.

# Web Framework
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# Data Validation (no pin — let ollama/langchain decide the compatible range)
pydantic>=2.9.0
pydantic-settings>=2.2.0

# Database
sqlalchemy>=2.0.0

# Auth / Security
bcrypt>=4.0.1
python-jose[cryptography]>=3.3.0

# Env
python-dotenv>=1.0.0

# Ollama Python client
ollama>=0.4.2

# LangChain — AI chain orchestration (replaces LangGraph per user request)
langchain>=0.2.16
langchain-core>=0.2.40
langchain-ollama>=0.1.3
langchain-community>=0.2.16

# Phase 3 — RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0
pypdf>=4.2.0

# Phase 4 — Notifications
python-telegram-bot>=21.3

# Scheduling (daily summaries)
apscheduler>=3.10.4
